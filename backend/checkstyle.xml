<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  FinTrack AI — Checkstyle Configuration
  Balanced rules: catches real issues without blocking velocity.
-->

<module name="Checker">
    <property name="severity" value="warning"/>
    <property name="charset" value="UTF-8"/>
    <property name="fileExtensions" value="java"/>

    <!-- Max file length: 1000 lines (reasonable) -->
    <module name="FileLength">
        <property name="max" value="1000"/>
    </module>

    <!-- No tabs allowed -->
    <module name="FileTabCharacter"/>

    <!-- Line length (Checker-level) -->
    <module name="LineLength">
        <property name="max" value="180"/>
        <property name="ignorePattern" value="^import .*|http://|https://"/>
    </module>

    <module name="TreeWalker">
        <!-- Naming conventions -->
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="MethodName"/>
        <module name="PackageName"/>
        <module name="ParameterName"/>
        <module name="StaticVariableName"/>
        <module name="TypeName"/>
        <module name="ConstantName"/>

        <!-- Imports -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>
        <module name="AvoidStarImport"/>

        <!-- Code structure -->
        <module name="ModifierOrder"/>
        <module name="RedundantModifier"/>
        <module name="NeedBraces">
            <property name="allowSingleLineStatement" value="true"/>
        </module>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="EmptyBlock"/>

        <!-- No empty catch blocks -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignore"/>
        </module>

        <!-- No empty statements -->
        <module name="EmptyStatement"/>

        <!-- One top-level class per file -->
        <module name="OneTopLevelClass"/>

        <!-- Override/Overload consistency -->
        <module name="MissingOverride"/>
    </module>
</module>
